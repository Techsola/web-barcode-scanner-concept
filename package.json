{
  "scripts": {
    "build": "yarn parcel build --public-url .",
    "serve": "yarn parcel serve"
  },
  "source": "src/index.html",
  "devDependencies": {
    "assert": "^2.0.0",
    "browserify-zlib": "^0.2.0",
    "events": "^3.3.0",
    "parcel": "^2.7.0",
    "path-browserify": "^1.0.1",
    "stream-browserify": "^3.0.0",
    "util": "^0.12.4"
  }
}
